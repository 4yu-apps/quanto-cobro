import 'package:flutter/material.dart';

import '../../core/ui/placeholder_screen.dart';

/// Perfis (Pro): alterna cenários de preço (cliente recorrente x avulso).
class PerfisScreen extends StatelessWidget {
  const PerfisScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(title: 'Perfis');
}
