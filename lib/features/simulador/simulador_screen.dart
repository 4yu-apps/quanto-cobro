import 'package:flutter/material.dart';

import '../../core/ui/placeholder_screen.dart';

/// Simulador de projeto (J3): lucro real + aviso comparativo se abaixo do alvo.
class SimuladorScreen extends StatelessWidget {
  const SimuladorScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(title: 'Vou orçar um projeto');
}
