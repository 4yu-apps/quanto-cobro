import 'package:flutter/material.dart';

import '../../core/ui/placeholder_screen.dart';

/// Detalhamento ("como cheguei aqui"): a conta linha a linha, editável.
class DetalheScreen extends StatelessWidget {
  const DetalheScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(title: 'Detalhamento');
}
