import 'package:flutter/material.dart';

import '../../core/ui/placeholder_screen.dart';

/// Reserva por pagamento (J2) — o caminho de ouro. Uso recorrente = hábito.
class ReservaScreen extends StatelessWidget {
  const ReservaScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(title: 'Recebi um pagamento');
}
