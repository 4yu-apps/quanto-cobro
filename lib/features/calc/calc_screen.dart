import 'package:flutter/material.dart';

import '../../core/ui/placeholder_screen.dart';

/// Calculadora guiada (5 passos) — setup estratégico do valor-hora.
class CalcScreen extends StatelessWidget {
  const CalcScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(title: 'Calculadora');
}
