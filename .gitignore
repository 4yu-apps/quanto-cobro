# ===== Segredos — NUNCA versionar (ver 4yu-apps/CLAUDE.md) =====
.env
.env.*
*.jks
*.keystore
key.properties
android/key.properties
# google-services.json NAO entra aqui de proposito: ele nao e segredo, vai
# dentro do APK e sem ele o firebase_core nao sobe (clone limpo nao compila).
# Ver docs/planning/11-HANDOFF.md §5.
ios/Runner/GoogleService-Info.plist
lib/firebase_options.dart

# ===== OS / editor / WSL =====
.DS_Store
Thumbs.db
*:Zone.Identifier
.thumbnail
.idea/
.vscode/
*.iml

# ===== Flutter / Dart =====
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
pubspec.lock.bak
*.g.dart.bak

# fvm — versiona só o .fvmrc (a versão pinada), não o SDK baixado
.fvm/

# scratch de execução por subagentes (ledger de progresso, briefs, reports)
.superpowers/

# ===== Android =====
android/.gradle/
android/local.properties
android/.kotlin/

# ===== iOS =====
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh

# ===== Desktop (só preview) =====
# O produto é Android. A pasta `linux/` existe pra rodar o app numa janela
# do WSLg durante o desenvolvimento (`flutter run -d linux`) — scaffold que
# o Flutter regenera sozinho e que ninguém aqui mantém. Versionar seria
# convidar alguém a tratar Linux como plataforma de entrega, que não é.
linux/
windows/
macos/
