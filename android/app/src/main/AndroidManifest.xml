<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Necessária pra buscar a cotação pública de câmbio (Fase 3) — sem ela
         o build de release não tem rede nenhuma e a busca falha em silêncio. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Lembrete de imposto (F7). POST_NOTIFICATIONS: obrigatória no Android 13+
         pra qualquer notificação (pedida em runtime pelo toggle dos Ajustes).
         RECEIVE_BOOT_COMPLETED: deixa o lembrete voltar depois de reiniciar o
         aparelho (o AlarmManager esquece os agendamentos no boot). NÃO pedimos
         SCHEDULE_EXACT_ALARM de propósito: agendamento inexato basta pra um
         lembrete mensal e evita a revisão extra da Play sobre alarme exato. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- O SDK do Firebase Analytics injeta a permissão AD_ID via merge e coleta
         o ID de publicidade por padrão. Este app não tem anúncio nenhum, então
         removemos a permissão E desligamos a coleta (meta-data abaixo). Assim a
         política de privacidade e o Data Safety podem dizer, com verdade, que o
         app NÃO coleta identificador de publicidade. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>

    <!-- Tela grande e janela livre: o app roda em tablet, dobravel e modo
         multi-janela, e o layout responde a isso (core/ui/breakpoints.dart).
         Os dois ja sao o default hoje, mas default e o tipo de coisa que uma
         versao do Gradle muda por voce — e o preco de descobrir e a Play
         Console marcando o app como "nao otimizado para telas grandes". -->
    <application
        android:label="Quanto Cobro?"
        android:name="${applicationName}"
        android:resizeableActivity="true"
        android:icon="@mipmap/ic_launcher">
        <meta-data
            android:name="android.supports_size_changes"
            android:value="true"/>
        <!-- Desliga a coleta do ID de publicidade pelo Analytics (ver a permissão
             AD_ID removida acima). App sem anúncio não precisa disso. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications (F7): o receiver que dispara a
             notificação agendada, e o que a REAGENDA depois de reiniciar o
             aparelho (ou de atualizar o app). Sem o boot receiver, o lembrete
             sumiria no primeiro reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
